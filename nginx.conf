server {
  listen 8080;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # React Routerの画面遷移はフロントエンド側で処理するため、
  # /items/1 のようなパスでも index.html を返す。
  location / {
    try_files $uri $uri/ /index.html;
  }
}
